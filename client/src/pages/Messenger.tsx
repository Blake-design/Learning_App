import React from "react";
import { MessengerApp } from "../components/messenger";

const Messenger = () => {
  return (
    <section>
      <MessengerApp />
    </section>
  );
};

export default Messenger;

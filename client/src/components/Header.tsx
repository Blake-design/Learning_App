import { LoginButton, ThemeButton } from "./";

const Header = () => {
  return (
    <header>
      <h3>The Learning App</h3>
      <ThemeButton />

      <LoginButton />
    </header>
  );
};

export default Header;
